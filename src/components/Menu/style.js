import styled from 'styled-components';
import Button from '../Button';

export const LogoImage = styled.img`
max-width: 168px; 
`;

export const MenuWrapper = styled.nav`
`;

export const ButtonLink = stytled(Button)`
`;